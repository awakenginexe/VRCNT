<Application x:Class="VRCNT.Setup.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:sys="clr-namespace:System;assembly=mscorlib"
             ShutdownMode="OnMainWindowClose">
    <Application.Resources>
        <!-- Obsidian Lumina Color Tokens -->
        <SolidColorBrush x:Key="InstallerCanvasBrush" Color="#0B0D13" />
        <SolidColorBrush x:Key="InstallerSurfaceBrush" Color="#141822" />
        <SolidColorBrush x:Key="InstallerSurfaceMutedBrush" Color="#1C2230" />
        <SolidColorBrush x:Key="InstallerSurfaceDropdownBrush" Color="#1A202C" />
        <SolidColorBrush x:Key="InstallerTextBrush" Color="#F8FAFC" />
        <SolidColorBrush x:Key="InstallerTextMutedBrush" Color="#94A3B8" />
        <SolidColorBrush x:Key="InstallerTextDimBrush" Color="#64748B" />
        <SolidColorBrush x:Key="InstallerBorderBrush" Color="#222A3A" />
        <SolidColorBrush x:Key="InstallerBorderHoverBrush" Color="#3A4760" />
        <SolidColorBrush x:Key="InstallerAccentBrush" Color="#00D2FF" />
        <SolidColorBrush x:Key="InstallerAccentHoverBrush" Color="#33DCFF" />
        <SolidColorBrush x:Key="InstallerAccentTextBrush" Color="#050B14" />
        <SolidColorBrush x:Key="InstallerFocusBrush" Color="#00D2FF" />
        <SolidColorBrush x:Key="InstallerSuccessBrush" Color="#10B981" />
        <SolidColorBrush x:Key="InstallerCudaAccentBrush" Color="#10B981" />
        <SolidColorBrush x:Key="InstallerCudaSurfaceBrush" Color="#121C1C" />
        <SolidColorBrush x:Key="InstallerErrorBrush" Color="#EF4444" />
        <SolidColorBrush x:Key="InstallerWarningBrush" Color="#F59E0B" />

        <!-- Dimension Tokens -->
        <sys:Double x:Key="InstallerBodyFontSize">13</sys:Double>
        <sys:Double x:Key="InstallerHeadingFontSize">22</sys:Double>
        <sys:Double x:Key="InstallerHeaderFontSize">15</sys:Double>
        <sys:Double x:Key="InstallerCardTitleFontSize">15</sys:Double>
        <sys:Double x:Key="InstallerControlMinHeight">36</sys:Double>
        <sys:Double x:Key="InstallerProgressHeight">6</sys:Double>
        <Thickness x:Key="InstallerPageMargin">24</Thickness>
        <Thickness x:Key="InstallerCardPadding">16</Thickness>
        <CornerRadius x:Key="InstallerButtonRadius">6</CornerRadius>
        <CornerRadius x:Key="InstallerCardRadius">8</CornerRadius>

        <!-- Base Window Style -->
        <Style TargetType="Window">
            <Setter Property="Background" Value="{DynamicResource InstallerCanvasBrush}" />
            <Setter Property="Foreground" Value="{DynamicResource InstallerTextBrush}" />
            <Setter Property="FontFamily" Value="Segoe UI Variable Text, Segoe UI" />
            <Setter Property="FontSize" Value="{DynamicResource InstallerBodyFontSize}" />
            <Setter Property="UseLayoutRounding" Value="True" />
            <Setter Property="TextOptions.TextFormattingMode" Value="Display" />
            <Setter Property="TextOptions.TextRenderingMode" Value="ClearType" />
        </Style>

        <!-- Base Button Style -->
        <Style TargetType="Button">
            <Setter Property="MinHeight" Value="{DynamicResource InstallerControlMinHeight}" />
            <Setter Property="Padding" Value="16,8" />
            <Setter Property="Margin" Value="0" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="FocusVisualStyle">
                <Setter.Value>
                    <Style TargetType="Control">
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="Control">
                                    <Border BorderBrush="{DynamicResource InstallerFocusBrush}" BorderThickness="1" CornerRadius="{DynamicResource InstallerButtonRadius}" Margin="-2" />
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Primary CTA Button Style -->
        <Style x:Key="PrimaryButtonStyle" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="Background" Value="{DynamicResource InstallerAccentBrush}" />
            <Setter Property="Foreground" Value="{DynamicResource InstallerAccentTextBrush}" />
            <Setter Property="BorderBrush" Value="{DynamicResource InstallerAccentBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="FontWeight" Value="Bold" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Border" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="{DynamicResource InstallerButtonRadius}" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="{DynamicResource InstallerAccentHoverBrush}" />
                                <Setter TargetName="Border" Property="BorderBrush" Value="{DynamicResource InstallerAccentHoverBrush}" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#00B4DB" />
                                <Setter TargetName="Border" Property="RenderTransform">
                                    <Setter.Value>
                                        <TranslateTransform Y="1" />
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="Border" Property="Background" Value="{DynamicResource InstallerSurfaceMutedBrush}" />
                                <Setter TargetName="Border" Property="BorderBrush" Value="{DynamicResource InstallerBorderBrush}" />
                                <Setter Property="Foreground" Value="{DynamicResource InstallerTextDimBrush}" />
                                <Setter Property="Cursor" Value="Arrow" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Secondary Ghost/Outline Button Style -->
        <Style x:Key="SecondaryButtonStyle" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="Background" Value="{DynamicResource InstallerSurfaceBrush}" />
            <Setter Property="Foreground" Value="{DynamicResource InstallerTextMutedBrush}" />
            <Setter Property="BorderBrush" Value="{DynamicResource InstallerBorderBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Border" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="{DynamicResource InstallerButtonRadius}" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="{DynamicResource InstallerSurfaceMutedBrush}" />
                                <Setter TargetName="Border" Property="BorderBrush" Value="{DynamicResource InstallerBorderHoverBrush}" />
                                <Setter Property="Foreground" Value="{DynamicResource InstallerTextBrush}" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Border" Property="RenderTransform">
                                    <Setter.Value>
                                        <TranslateTransform Y="1" />
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="Border" Property="Opacity" Value="0.45" />
                                <Setter Property="Cursor" Value="Arrow" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- CPU Runtime Card Style -->
        <Style x:Key="RuntimeCardStyle" TargetType="RadioButton">
            <Setter Property="Foreground" Value="{DynamicResource InstallerTextBrush}" />
            <Setter Property="Padding" Value="{DynamicResource InstallerCardPadding}" />
            <Setter Property="Margin" Value="0" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="FocusVisualStyle">
                <Setter.Value>
                    <Style TargetType="Control">
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="Control">
                                    <Border BorderBrush="{DynamicResource InstallerAccentBrush}" BorderThickness="1" CornerRadius="{DynamicResource InstallerCardRadius}" Margin="-2" />
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </Setter.Value>
            </Setter>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RadioButton">
                        <Border x:Name="Border" Background="{DynamicResource InstallerSurfaceBrush}" BorderBrush="{DynamicResource InstallerBorderBrush}" BorderThickness="1" CornerRadius="{DynamicResource InstallerCardRadius}" Padding="{TemplateBinding Padding}">
                            <ContentPresenter />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="Border" Property="BorderBrush" Value="{DynamicResource InstallerAccentBrush}" />
                                <Setter TargetName="Border" Property="Background" Value="#111A29" />
                            </Trigger>
                            <MultiTrigger>
                                <MultiTrigger.Conditions>
                                    <Condition Property="IsMouseOver" Value="True" />
                                    <Condition Property="IsChecked" Value="False" />
                                </MultiTrigger.Conditions>
                                <Setter TargetName="Border" Property="Background" Value="#181E2B" />
                                <Setter TargetName="Border" Property="BorderBrush" Value="{DynamicResource InstallerBorderHoverBrush}" />
                            </MultiTrigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="Border" Property="Opacity" Value="0.45" />
                                <Setter Property="Cursor" Value="Arrow" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- CUDA Runtime Card Style -->
        <Style x:Key="CudaRuntimeCardStyle" TargetType="RadioButton" BasedOn="{StaticResource RuntimeCardStyle}">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RadioButton">
                        <Border x:Name="Border" Background="{DynamicResource InstallerSurfaceBrush}" BorderBrush="{DynamicResource InstallerBorderBrush}" BorderThickness="1" CornerRadius="{DynamicResource InstallerCardRadius}" Padding="{TemplateBinding Padding}">
                            <ContentPresenter />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="Border" Property="BorderBrush" Value="{DynamicResource InstallerCudaAccentBrush}" />
                                <Setter TargetName="Border" Property="Background" Value="{DynamicResource InstallerCudaSurfaceBrush}" />
                            </Trigger>
                            <MultiTrigger>
                                <MultiTrigger.Conditions>
                                    <Condition Property="IsMouseOver" Value="True" />
                                    <Condition Property="IsChecked" Value="False" />
                                </MultiTrigger.Conditions>
                                <Setter TargetName="Border" Property="Background" Value="#181E2B" />
                                <Setter TargetName="Border" Property="BorderBrush" Value="{DynamicResource InstallerBorderHoverBrush}" />
                            </MultiTrigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="Border" Property="Opacity" Value="0.45" />
                                <Setter Property="Cursor" Value="Arrow" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Custom CheckBox Style -->
        <Style TargetType="CheckBox">
            <Setter Property="Foreground" Value="{DynamicResource InstallerTextMutedBrush}" />
            <Setter Property="FontSize" Value="12" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="CheckBox">
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                            <Border x:Name="Box" Width="16" Height="16" Background="{DynamicResource InstallerCanvasBrush}" BorderBrush="{DynamicResource InstallerBorderBrush}" BorderThickness="1" CornerRadius="4" VerticalAlignment="Center" Margin="0,0,8,0">
                                <Path x:Name="CheckGlyph" Data="M3 8 L6 11 L13 4" Stroke="{DynamicResource InstallerAccentBrush}" StrokeThickness="2" StrokeStartLineCap="Round" StrokeEndLineCap="Round" Visibility="Collapsed" Margin="1" />
                            </Border>
                            <ContentPresenter VerticalAlignment="Center" />
                        </StackPanel>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="CheckGlyph" Property="Visibility" Value="Visible" />
                                <Setter TargetName="Box" Property="BorderBrush" Value="{DynamicResource InstallerAccentBrush}" />
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Foreground" Value="{DynamicResource InstallerTextBrush}" />
                                <Setter TargetName="Box" Property="BorderBrush" Value="{DynamicResource InstallerBorderHoverBrush}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Custom ProgressBar Style -->
        <Style TargetType="ProgressBar">
            <Setter Property="Height" Value="{DynamicResource InstallerProgressHeight}" />
            <Setter Property="Background" Value="{DynamicResource InstallerSurfaceMutedBrush}" />
            <Setter Property="Foreground" Value="{DynamicResource InstallerAccentBrush}" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ProgressBar">
                        <Grid x:Name="TemplateRoot">
                            <Border Background="{TemplateBinding Background}" CornerRadius="3" />
                            <Rectangle x:Name="PART_Track" />
                            <Grid x:Name="PART_Indicator" ClipToBounds="True" HorizontalAlignment="Left">
                                <Border x:Name="Indicator" Background="{TemplateBinding Foreground}" CornerRadius="3" />
                            </Grid>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Custom ComboBox Style for Header Language Dropdown -->
        <Style TargetType="ComboBox">
            <Setter Property="Background" Value="{DynamicResource InstallerSurfaceBrush}" />
            <Setter Property="Foreground" Value="{DynamicResource InstallerTextBrush}" />
            <Setter Property="BorderBrush" Value="{DynamicResource InstallerBorderBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="MinHeight" Value="28" />
            <Setter Property="Padding" Value="8,4" />
            <Setter Property="FontSize" Value="12" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="ItemContainerStyle">
                <Setter.Value>
                    <Style TargetType="ComboBoxItem">
                        <Setter Property="Background" Value="Transparent" />
                        <Setter Property="Foreground" Value="{DynamicResource InstallerTextBrush}" />
                        <Setter Property="Padding" Value="10,6" />
                        <Setter Property="Cursor" Value="Hand" />
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="ComboBoxItem">
                                    <Border x:Name="ItemBorder" Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}" CornerRadius="4" Margin="2">
                                        <ContentPresenter />
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="ItemBorder" Property="Background" Value="{DynamicResource InstallerSurfaceMutedBrush}" />
                                            <Setter Property="Foreground" Value="{DynamicResource InstallerAccentBrush}" />
                                        </Trigger>
                                        <Trigger Property="IsSelected" Value="True">
                                            <Setter TargetName="ItemBorder" Property="Background" Value="{DynamicResource InstallerSurfaceMutedBrush}" />
                                            <Setter Property="Foreground" Value="{DynamicResource InstallerAccentBrush}" />
                                            <Setter Property="FontWeight" Value="SemiBold" />
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </Setter.Value>
            </Setter>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <ToggleButton x:Name="ToggleButton" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" Focusable="False" IsChecked="{Binding Path=IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}" ClickMode="Press">
                                <ToggleButton.Template>
                                    <ControlTemplate TargetType="ToggleButton">
                                        <Border x:Name="Border" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="6" Padding="8,4">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*" />
                                                    <ColumnDefinition Width="Auto" />
                                                </Grid.ColumnDefinitions>
                                                <ContentPresenter Grid.Column="0" Content="{TemplateBinding Content}" ContentTemplate="{TemplateBinding ContentTemplate}" HorizontalAlignment="Left" VerticalAlignment="Center" Margin="0,0,6,0" />
                                                <Path Grid.Column="1" Data="M0 0 L4 4 L8 0 Z" Fill="{DynamicResource InstallerTextMutedBrush}" HorizontalAlignment="Right" VerticalAlignment="Center" />
                                            </Grid>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="Border" Property="BorderBrush" Value="{DynamicResource InstallerBorderHoverBrush}" />
                                                <Setter TargetName="Border" Property="Background" Value="{DynamicResource InstallerSurfaceMutedBrush}" />
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </ToggleButton.Template>
                            </ToggleButton>
                            <ContentPresenter x:Name="ContentSite" IsHitTestVisible="False" Content="{TemplateBinding SelectionBoxItem}" ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}" ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}" Margin="8,4,20,4" VerticalAlignment="Center" HorizontalAlignment="Left" />
                            <Popup x:Name="Popup" Placement="Bottom" IsOpen="{TemplateBinding IsDropDownOpen}" AllowsTransparency="True" Focusable="False" PopupAnimation="Slide">
                                <Grid x:Name="DropDown" SnapsToDevicePixels="True" MinWidth="{TemplateBinding ActualWidth}" MaxHeight="{TemplateBinding MaxDropDownHeight}">
                                    <Border x:Name="DropDownBorder" Background="{DynamicResource InstallerSurfaceDropdownBrush}" BorderThickness="1" BorderBrush="{DynamicResource InstallerBorderHoverBrush}" CornerRadius="6" Margin="0,2,0,0" Padding="2">
                                        <ScrollViewer SnapsToDevicePixels="True">
                                            <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Contained" />
                                        </ScrollViewer>
                                    </Border>
                                </Grid>
                            </Popup>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Application.Resources>
</Application>
